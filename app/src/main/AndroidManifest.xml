<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <permission
        android:name="com.kiddolock.app.ADMIN"
        android:protectionLevel="signature" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name=".SafeLockApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SafeLock"
        tools:targetApi="31">

        <receiver
            android:name=".receivers.KiddoDeviceAdminReceiver"
            android:description="@string/device_admin_description"
            android:label="@string/device_admin_label"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
                <action android:name="android.app.action.PROFILE_PROVISIONING_COMPLETE"/>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <receiver
            android:name=".receivers.BootReceiver"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <service
            android:name=".services.SafeLockAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:foregroundServiceType="dataSync"
            android:isAccessibilityTool="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SafeLock">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.SetupActivity"
            android:exported="false"
            android:label="@string/setup_title"
            android:theme="@style/Theme.SafeLock" />

        <activity
            android:name=".ui.AdminActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.SafeLock" />

        <activity
            android:name=".ui.ParentInsightsActivity"
            android:exported="false"
            android:label="@string/weekly_report_channel_name"
            android:theme="@style/Theme.SafeLock" />

        <activity
            android:name=".ui.AdminPinActivity"
            android:exported="false"
            android:label="@string/admin_pin_title"
            android:theme="@style/Theme.SafeLock"
            android:windowSoftInputMode="stateVisible" />

        <activity
            android:name=".ui.HelpActivity"
            android:exported="false"
            android:label="@string/permission_explanation_title"
            android:theme="@style/Theme.SafeLock" />

        <activity
            android:name=".ui.ContentFilterActivity"
            android:exported="false"
            android:label="@string/content_filter_title"
            android:theme="@style/Theme.SafeLock" />

        <activity
            android:name=".ui.BlockedActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:taskAffinity=""
            android:theme="@style/Theme.SafeLock.Overlay" />

        <receiver
            android:name=".receivers.EmergencyReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.kiddolock.app.EMERGENCY_UNLOCK" />
                <action android:name="com.kiddolock.app.EMERGENCY_DISABLE" />
                <action android:name="com.kiddolock.app.EMERGENCY_UNINSTALL" />
                <action android:name="com.kiddolock.app.TEST_TRIGGER_BEDTIME" />
                <action android:name="com.kiddolock.app.TEST_SET_INSTANT_LOCK" />
                <action android:name="com.kiddolock.app.TEST_DUMP_STATE" />
            </intent-filter>
        </receiver>

        <service
            android:name=".ui.OverlayService"
            android:exported="false" />

    </application>
</manifest>
